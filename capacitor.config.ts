import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jfiexpress.JFIExpress',
  appName: 'JFI Express',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;

import { PackageHistoryData } from './package-history-data';

describe('PackageHistoryData', () => {
  it('should create an instance', () => {
    expect(new PackageHistoryData()).toBeTruthy();
  });
});
